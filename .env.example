# Local HTTP Bridge MCP Server - Environment Variables
#
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# DO NOT commit .env to version control!

# ============================================================================
# API Authentication Tokens
# ============================================================================

# Bearer tokens for API authentication
API_HVS_TOKEN=your_api_hvs_token_here
APEX_DEMO_TOKEN=your_apex_demo_token_here

# Custom API keys
CUSTOM_API_KEY=your_custom_api_key_here

# ============================================================================
# Basic Authentication
# ============================================================================

# For APIs requiring Basic Auth
INTERNAL_API_USER=your_username
INTERNAL_API_PASS=your_password

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Log file path (default: stderr)
# LOG_FILE=/var/log/local-http-bridge/traffic.log

# Enable HTTP traffic logging (true/false)
ENABLE_HTTP_LOGGING=true

# ============================================================================
# Server Configuration
# ============================================================================

# Default timeout for requests (seconds)
DEFAULT_TIMEOUT=30

# Maximum response size (bytes)
MAX_RESPONSE_SIZE=10485760  # 10MB

# Maximum number of redirects to follow
MAX_REDIRECTS=5

# ============================================================================
# Security Settings
# ============================================================================

# Enable SSL verification by default (true/false)
DEFAULT_SSL_VERIFY=true

# Enable response sanitization (redact sensitive data)
ENABLE_RESPONSE_SANITIZATION=true

# ============================================================================
# Advanced Features (see advanced_config_example.py)
# ============================================================================

# Enable request caching (true/false)
ENABLE_CACHING=false

# Default cache TTL (seconds)
DEFAULT_CACHE_TTL=60

# Enable rate limiting (true/false)
ENABLE_RATE_LIMITING=false

# Default rate limit (requests per minute)
DEFAULT_RATE_LIMIT=100

# ============================================================================
# Domain-Specific Configuration
# ============================================================================

# You can add domain-specific environment variables here
# These can be referenced in advanced_config_example.py

# Example: Custom timeout for specific domains
SLOW_API_TIMEOUT=120
FAST_API_TIMEOUT=5

# Example: Domain-specific SSL settings
DEV_LOCAL_SSL_VERIFY=false
STAGING_HVS_SSL_VERIFY=false

# ============================================================================
# Docker-Specific Settings
# ============================================================================

# Container timezone (optional)
TZ=UTC

# User/Group IDs for non-root execution (optional)
# PUID=1000
# PGID=1000

# ============================================================================
# Monitoring & Observability (future features)
# ============================================================================

# Prometheus metrics endpoint (if enabled)
# METRICS_ENABLED=false
# METRICS_PORT=9090

# Sentry DSN for error tracking (if enabled)
# SENTRY_DSN=your_sentry_dsn_here

# ============================================================================
# Notes
# ============================================================================
#
# 1. Security Best Practices:
#    - NEVER commit the .env file to version control
#    - Use strong, unique tokens for each environment
#    - Rotate tokens regularly
#    - Use a secrets manager in production (AWS Secrets Manager, HashiCorp Vault, etc.)
#
# 2. Loading Environment Variables:
#    - From Python: Use python-dotenv
#      from dotenv import load_dotenv
#      load_dotenv()
#    - From Docker: Use --env-file .env flag
#    - From Docker Compose: Automatically loaded if named .env
#
# 3. Production Deployment:
#    - Use environment-specific .env files (.env.production, .env.staging, etc.)
#    - Inject secrets via CI/CD pipelines
#    - Consider using encrypted secret storage
#
# 4. Validation:
#    - The server will use default values if env vars are not set
#    - Check logs on startup to verify configuration is loaded correctly
